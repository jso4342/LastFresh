/*
$('#Type').each(function (i,btn) {
    $(btn).on("click",function () {
        $("#Type").toggleClass("on");
        $(".orders_form").hide();
        if($("#Type").hasClass("on")){
            $(".orders_form").show();
        }
    })
})
$('#Payment').each(function (i,btn) {
    $(btn).on("click",function () {
        $("#Payment").toggleClass("on");
        $(".PaymentShow").hide();
        if($("#Payment").hasClass("on")){
            $(".PaymentShow").show();
        }
    })
})
$('#OrderInform').each(function (i,btn) {
    $(btn).on("click",function () {
        $("#OrderInform").toggleClass("on");
        $(".OrderInformShow").hide();
        if($("#OrderInform").hasClass("on")){
            $(".OrderInformShow").show();
        }
    })
})
$('#ShippingInform').each(function (i,btn) {
    $(btn).on("click",function () {
        $("#ShippingInform").toggleClass("on");
        $(".ShippingInformShow").hide();
        if($("#ShippingInform").hasClass("on")){
            $(".ShippingInformShow").show();
        }
    })
})*/
